## **주요 문제 해결 및 기술 리서치 (테스트 단계)**

테스트 과정에서 발견된 엣지 케이스(Edge Case)와 성능 병목을 해결하기 위해 리서치하고 적용한 방법을 기록합니다.

| **이슈 구분**   | **문제 상황 및 원인**                         | **리서치 및 해결 과정 (Reference & Solution)**                                                                                 |
| ----------- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **검색 품질**   | 법령 RAG 질의 126건 중 71건(약 56%)이 하위질문 1개 이상에서 `Empty Response` 반환, 동일 질문 반복 시 답변이 상반되는 사례까지 확인(`PoC_모듈_구현_답완_v2.md` 4장·5.4절). 원인은 ①`LawChunker`가 조문/별표 전체를 parent 노드 1개로 묶어 노드가 페이지 단위로 비대해지고 임베딩 품질이 저하된 점, ②retriever가 `similarity_top_k=10` + `SimilarityPostprocessor(cutoff=0.5)`로 관련 조문을 조기에 걸러낸 점 | • **리서치:** LlamaIndex retriever 파라미터(top_k, similarity cutoff, rerank)가 리콜에 미치는 영향과 parent/child 청크 크기가 임베딩 품질에 미치는 영향 조사 • **적용:** `LawChunker`에 크기 상한(`_MAX_PARENT_CHARS=1200`, `_MAX_CHILD_CHARS=400`)을 도입해 큰 조문은 항(①②③) 단위로, 큰 별표는 소제목 단위로 parent를 추가 분할하고 그래도 크면 문장 단위로 재분할. retriever는 `similarity_top_k` 10→50, `SimilarityPostprocessor` cutoff 제거, `LLMRerank top_n` 2→3으로 조정 (`llm-mcp-doc-rag/utils/chunkingPatterns/lawChunker.py`, `llm-mcp-doc-rag/utils/ragUtils.py`, 커밋 전 작업 중) |
| **속도/지연**   | 한 `agent_mode`에 리소스(zip)가 여러 개 등록된 경우(예: 기존 `PpsConstructionAgent`는 공통/입찰참가자격사전심사기준/적격심사세부기준/종합심사 4개 zip), `execute_rag` 노드가 엔진별로 순차 `aquery` 호출을 해 지연시간이 엔진 개수만큼 그대로 누적됨 | • **리서치:** `asyncio.gather` 기반 병렬 함수 호출 패턴 조사 • **적용:** (엔진, 하위질문) 조합 전체를 한 번에 `gather`로 병렬 실행하도록 `execute_rag` 수정. 더불어 `PpsConstructionAgent`의 4개 zip을 1개 통합 zip으로 교체하고, 상담 빈도가 낮은 `PpsGeneralServiceAgent`·`PpsTechnicalServicesAgent`·`PpsProductsAgent`는 리소스 로딩 자체를 비활성화해 엔진 개수를 축소 (`llm-mcp-doc-rag/tools/docAnalyze.py`, `llm-mcp-doc-rag/main.py`) |
| **설정 정합성/가드레일** | 위 리소스 비활성화 작업 직후 실사용 로그에서 발견: 리소스를 껐는데도 `agent_mode` 자동 선택 LLM은 여전히 `PpsTechnicalServicesAgent`를 선택할 수 있고, `config.get_query_engine()`이 `self._query_engines[mode]`로 딕셔너리에 직접 접근해 미등록 모드 요청 시 `KeyError`가 발생 → RAG가 컨텍스트 없이 실패하고 사용자에게는 근거 없는 사전지식 답변만 노출됨 | • **리서치:** 원인 로그(`RAG Query Engine creation failed: 'PpsTechnicalServicesAgent'`) 추적 • **현재 상태:** 아직 미해결. "소프트웨어 유지보수 용역 하도급 제한" 질문을 2026-07-05에 2회 질의했고 2회 모두 동일하게 실패함(`log/llm-server_2026-07-05.log`, `log/llm-mcp-doc-rag_2026-07-05.log`). `.get(mode, [])` 형태의 방어 코드 또는 agent_mode 선택 프롬프트에 현재 활성 모드 목록만 명시하는 조치가 다음 고도화 과제로 남음. 참고로 프롬프트 인젝션 등 보안 성격의 가드레일은 이번 테스트 단계에서 별도로 검증되지 않음 |

## 1. LLM 답변 품질 평가 및 개선

| 항목       | 내용                                                                  |
| -------- | ------------------------------------------------------------------- |
| 평가 대상 기능 | 법령 RAG 검색·답변 (`PpsAssistAgent`, `qna_law_base` 도구)                    |
| 평가 방식    | 별도 Ground Truth 세트 없이, 실제 운영 로그(`log/llm-mcp-doc-rag_2026-06-05.log` ~ `2026-07-05.log`, 18개 파일)의 `Final Response` 라인을 전수 집계 |
| 평가 결과 (개선 전) | 전체 126건 중 71건(약 56%)이 하위질문 1개 이상에서 `Empty Response`, 완전 성공 55건(약 44%). 동일 질문("추정가격 30억 공사는 어떤 적격심사?")을 8분 사이 6회 반복 질의한 로그에서 3회 Empty, 1회는 정반대 결론이 나오는 등 재현성도 낮음 |
| 원인 분석    | `LawChunker`가 조문/별표 전체를 parent 노드 하나로 묶어 노드가 비대해짐 + retriever의 `top_k=10`과 `SimilarityPostprocessor(cutoff=0.5)`가 관련 조문을 과도하게 걸러냄 |
| 개선 조치    | `LawChunker`에 부모/자식 크기 상한 도입(`_MAX_PARENT_CHARS=1200`, `_MAX_CHILD_CHARS=400`) — 큰 조문/별표를 항·소제목 단위로 추가 분할. retriever는 `similarity_top_k` 10→50, cutoff 제거, `LLMRerank top_n` 2→3 |
| 개선 후 결과  | 코드 변경은 완료되었으나 아직 커밋 전(uncommitted) 상태이며, 변경 이후 재수집된 운영 로그가 없어 `Empty Response` 비율 재측정은 다음 테스트 사이클에서 확인 필요 — **미추론 원칙에 따라 개선 수치는 현재 기재하지 않음** |

## 2. 성능 및 비용 최적화

| 항목    | 내용                                       |
| ----- | ---------------------------------------- |
| 기존 병목 | `agent_mode` 하나에 리소스(zip)가 여러 개 등록되면(예: 기존 `PpsConstructionAgent`의 4개 zip) `execute_rag`가 엔진별로 순차 `aquery` 호출을 해 지연시간이 엔진 개수만큼 누적 |
| 개선 전략 | (1) 쿼리 실행 자체를 병렬화 (2) 에이전트별 리소스 개수를 축소 |
| 적용 기술 | `asyncio.gather`로 (엔진, 하위질문) 전체 조합을 한 번에 병렬 실행하도록 `execute_rag` 수정(`docAnalyze.py`). `main.py`에서 `PpsConstructionAgent`의 4개 zip을 1개 통합 zip으로 교체하고, 상담 빈도가 낮은 `PpsGeneralServiceAgent`·`PpsTechnicalServicesAgent`·`PpsProductsAgent`는 리소스 로딩에서 제외 |
| 개선 결과 | 코드 반영은 완료. 단, 정량적 응답시간 비교에는 변경 전후 로그가 모두 필요한데 변경 전 로그(예: `log/llm-server_2026-07-03.log`, 처리시간 13.37초)만 확보되어 있어 변경 후 처리시간 재측정은 아직 없음 |

## 3. 예외 처리 및 가드레일

| 항목     | 내용                              |
| ------ | ------------------------------- |
| 예외 대상  | 리소스 로딩에서 제외된 `agent_mode`(`PpsGeneralServiceAgent`/`PpsTechnicalServicesAgent`/`PpsProductsAgent`)로 라우팅 LLM이 여전히 선택을 시도하는 경우 |
| 탐지 방식  | 사전 설계된 탐지 로직 없음 — 실사용 로그 모니터링 중 우연히 발견 |
| 대응 로직  | 현재 없음. `config.get_query_engine()`이 `self._query_engines[mode]`로 딕셔너리에 직접 접근해 미등록 모드 요청 시 `KeyError` 발생 → 예외가 상위에서 로그만 남기고 삼켜져 RAG는 컨텍스트 없이 종료, 사용자에게는 근거 문서 없는 사전지식 답변만 노출됨 |
| 테스트 결과 | "소프트웨어 유지보수 용역 발주 시 하도급 제한 기준" 질문을 2026-07-05에 2회 질의, 2회 모두 `PpsTechnicalServicesAgent`로 라우팅되어 100% 실패(`log/llm-server_2026-07-05.log`, `log/llm-mcp-doc-rag_2026-07-05.log`: `RAG Query Engine creation failed: 'PpsTechnicalServicesAgent'`) |
| 상태     | **미해결** — `.get(mode, [])` 형태의 방어 코드 추가 또는 agent_mode 자동 선택 프롬프트에 현재 활성 모드 목록만 노출하는 조치 필요(다음 고도화 과제). 프롬프트 인젝션·시스템 프롬프트 노출 시도 등 보안 성격의 가드레일 테스트는 이번 단계에서 별도로 수행되지 않음 |

## 4. 기타 문제 해결 사례

| 항목 | 내용 |
| --- | --- |
| 질문 분해(`decompose_question`) 로그 오표기 | 현재 작업 중인 diff에서 `try` 성공/실패 여부와 무관하게 항상 "Decomposition failed, using original question: ..."을 출력하도록 로그 라인이 추가되어, 실제로는 정상 분해된 경우에도 로그만 보면 실패로 오인됨(`log/llm-server_2026-07-05.log:16-17`, `log/llm-mcp-doc-rag_2026-07-05.log:16-17` 참고). 실제 동작 오류는 아니며 로그 메시지 표기만 부정확한 상태로, 커밋 전 정리 필요 |
| `docAnalyze.py` 내 중복 코드 | `execute_rag` 함수의 이전(순차 처리) 버전이 삼중따옴표 문자열로 주석 처리되어 두 벌 남아 있음 — 동작에는 영향 없으나 커밋 전 정리 대상 |
